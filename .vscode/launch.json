// .vscode/launch.json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run (Active CMake preset)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${command:cmake.getLaunchTargetDirectory}",
      "console": "integratedTerminal",
      "preLaunchTask": "CMake: build"
    }
  ]
}